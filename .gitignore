# phpstorm project files
.idea

# netbeans project files
nbproject

# zend studio for eclipse project files
.buildpath
.project
.settings

# windows thumbnail cache
Thumbs.db

# composer vendor dir
/vendor

# web/assets, which will be a symlink created by the deployment script
/web/assets

# composer itself is not needed
composer.phar

# Mac DS_Store Files
.DS_Store

# phpunit itself is not needed
phpunit.phar
# local phpunit config
/phpunit.xml

# files that are used locally, but are handled through develop / production split by the deployment script
/web/index.php
/config/db.php
/config/db-migrate.php
